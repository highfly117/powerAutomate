{
  

  "scripts": {
    "init-Server": "cd api && npm install && node ./bin/www",
    "init-Front": "cd ../ && cd client && npm install && npm run start",
    "start": "npom run init-Server && init-Front"
  }
}
